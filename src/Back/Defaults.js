/**
 * Plugin constants (hardcoded configuration) for backend code.
 */
export default class Dialog_Back_Defaults {
    NAME = '@flancer64/tg-demo-dialog';

    constructor() {
        Object.freeze(this);
    }
}
